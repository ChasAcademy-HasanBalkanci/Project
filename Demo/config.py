# Because of the credantials, we need to store them in a separate file and import them here. 
# We can use Hashicorp Vault, AWS secret manager, Azure Key Vault etc.

SENDGRID_API_KEY = 'your_sendgrid_api_key'
EMAIL_FROM = 'your_email@example.com'
EMAIL_TO = 'recipient@example.com'
